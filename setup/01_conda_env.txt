We create a conda environment for each project. As the current stable python versions are 3.13 and 3.14, we will use python 3.14.

This is the command we will use to create the conda environments.

    conda create --name demo_devops python=3.14

Before creating the environment make sure it doesn't already exist by listing the current environments:
    
    conda env list



